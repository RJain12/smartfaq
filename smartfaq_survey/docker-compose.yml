# Local Redis for development: docker compose up -d
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - smartfaq_redis:/data
    command: redis-server --appendonly yes

volumes:
  smartfaq_redis:
